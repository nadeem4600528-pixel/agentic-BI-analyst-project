import { PanelTitle, Table } from './Common'
export default function DataView({ title, records, result, action, actionLabel }) { return <section><div className="section-heading"><div><span className="tag">INTELLIGENCE</span><h2>{title}</h2><p>Use the active dataset to generate actionable findings.</p></div><button className="btn btn-primary" onClick={action}>{actionLabel} ↗</button></div><div className="panel"><PanelTitle title="Dataset records" sub={`${records.length} visible rows`} /><Table records={records} /></div>{result && <div className="panel mt-4"><PanelTitle title="Service result" sub="Returned by FastAPI" /><pre className="result-preview large">{JSON.stringify(result, null, 2)}</pre></div>}</section> }
