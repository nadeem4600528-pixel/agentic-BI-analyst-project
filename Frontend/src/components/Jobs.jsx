import { PanelTitle } from './Common'
import { workflows } from '../api'
export default function Jobs({ run, result }) { return <section><div className="section-heading"><div><span className="tag">AUTOMATION</span><h2>Workflow status.</h2><p>Monitor recent analytical jobs and pipeline health.</p></div><button className="btn btn-primary" onClick={() => run(() => workflows())}>Refresh jobs ↻</button></div><div className="panel"><PanelTitle title="Pipeline health" sub="In-memory workflow status" /><div className="job-row"><span className="job-status success">✓</span><div><strong>Workspace ready</strong><p>Local API connection available</p></div><span className="ms-auto text-muted">Ready</span></div>{result && <pre className="result-preview large">{JSON.stringify(result, null, 2)}</pre>}</div></section> }
