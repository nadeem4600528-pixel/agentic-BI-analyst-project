import { report } from '../api'
export default function Reports({ payload, download, run, result }) { return <section><div className="section-heading"><div><span className="tag">DELIVERABLES</span><h2>Reports that move work forward.</h2><p>Generate a consolidated view or download it for sharing.</p></div><button className="btn btn-primary" onClick={() => run(() => report(payload))}>Generate report</button></div><div className="report-grid">{[['profiling','Profiling report'],['cleaning','Cleaning report'],['analysis','Analysis report'],['consolidated','Consolidated report']].map(([id, title]) => <div className="report-card" key={id}><span className="report-symbol">▤</span><h3>{title}</h3><p>Quality, findings, insights, and recommendations.</p><button className="btn btn-outline-primary btn-sm" onClick={() => id === 'consolidated' ? download('pdf') : run(() => report(payload))}>Open report</button></div>)}</div><div className="export-bar"><div><strong>Export consolidated report</strong><p>Choose a format for offline sharing.</p></div><div className="d-flex gap-2"><button className="btn btn-outline-dark" onClick={() => download('excel')}>Excel</button><button className="btn btn-outline-dark" onClick={() => download('html')}>HTML</button><button className="btn btn-dark" onClick={() => download('pdf')}>PDF ↓</button></div></div>{result && <pre className="result-preview large mt-4">{JSON.stringify(result, null, 2)}</pre>}</section> }
