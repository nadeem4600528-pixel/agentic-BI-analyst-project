export default function Sidebar({ navigation, page, setPage }) { return <aside className="sidebar"><div className="brand"><span className="brand-mark">✦</span><div><strong>Agentic BI</strong><small>ANALYST STUDIO</small></div></div><div className="nav-label">WORKSPACE</div>{navigation.map(([id, icon, label]) => <button key={id} className={`nav-item ${page === id ? 'active' : ''}`} onClick={() => setPage(id)}><span>{icon}</span>{label}</button>)}<div className="sidebar-footer"><span className="status-dot" /> API ready<div className="version">v1.0 · Local workspace</div></div></aside> }
