import { PanelTitle, Table } from './Common'
export default function Upload({ fileName, upload, records }) { return <section><div className="section-heading"><div><span className="tag">DATA INGESTION</span><h2>Bring your data to life.</h2><p>Upload a file and the analyst will profile it automatically.</p></div></div><div className="upload-card"><div className="upload-icon">↥</div><h3>Drop your dataset here</h3><p>CSV, Excel, JSON, or Parquet · secure local processing</p><label className="btn btn-primary">Choose file<input type="file" hidden accept=".csv,.xlsx,.xls,.json,.parquet" onChange={upload} /></label><div className="upload-name">{fileName}</div></div><div className="panel mt-4"><PanelTitle title="Current preview" sub={`${records.length} records loaded`} /><Table records={records} /></div></section> }
